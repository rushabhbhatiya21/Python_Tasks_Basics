#  1. Create a function using 'lambda' for exponent of a given number. Take two
# arguments one as the number and the other one as the exponent

get_exponent = lambda x, y: x ** y
print(get_exponent(2, 5))
